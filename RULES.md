- DRY: Don't Repeat Yourself. Any code text shouldn't repeat
- Currently, a project uses SQLite, so use as few tables as possible, better to add columns to the existing table
  than creating a new one
